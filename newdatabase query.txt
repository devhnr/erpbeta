ALTER TABLE `warehouses` ADD `capacity_details` INT(11) NULL DEFAULT NULL AFTER `general_details`, ADD `total_area` VARCHAR(255) NULL DEFAULT NULL AFTER `capacity_details`, ADD `total_area_type` INT(11) NULL DEFAULT NULL COMMENT '1=Sq Feet,2=CBM' AFTER `total_area`, ADD `maximum_stack_height` VARCHAR(255) NULL DEFAULT NULL AFTER `total_area_type`, ADD `maximum_stack_height_type` VARCHAR(255) NULL DEFAULT NULL COMMENT '1=Feet,2=Meter' AFTER `maximum_stack_height`, ADD `pickup_area` VARCHAR(255) NULL DEFAULT NULL AFTER `maximum_stack_height_type`, ADD `pickup_area_type` VARCHAR(255) NULL DEFAULT NULL COMMENT '1=Sq Feet,2=CBM' AFTER `pickup_area`, ADD `receiving_area` VARCHAR(255) NULL DEFAULT NULL AFTER `pickup_area_type`, ADD `receiving_area_type` VARCHAR(255) NULL DEFAULT NULL COMMENT '1=Sq Feet,2=CBM' AFTER `receiving_area`, ADD `dispatch_area` VARCHAR(255) NULL DEFAULT NULL AFTER `receiving_area_type`, ADD `dispatch_area_type` VARCHAR(255) NULL DEFAULT NULL COMMENT '1=Sq Feet,2=CBM' AFTER `dispatch_area`, ADD `loading_unloading` VARCHAR(255) NULL DEFAULT NULL AFTER `dispatch_area_type`, ADD `loading_unloading_type` VARCHAR(255) NULL DEFAULT NULL COMMENT '1=Sq Feet,2=CBM' AFTER `loading_unloading`, ADD `storage_area` VARCHAR(255) NULL DEFAULT NULL AFTER `loading_unloading_type`, ADD `storage_area_type` VARCHAR(255) NULL DEFAULT NULL COMMENT '1=Sq Feet,2=CBM' AFTER `storage_area`, ADD `storage_capacity` VARCHAR(255) NULL DEFAULT NULL AFTER `storage_area_type`, ADD `used_capacity` VARCHAR(255) NULL DEFAULT NULL AFTER `storage_capacity`, ADD `available_capacity` VARCHAR(255) NULL DEFAULT NULL AFTER `used_capacity`;